{
  "_why": "State that SHOULD reset when App re-keys a tab. Anything not listed here must live in that component's view-state interface, or test:viewstate fails. Adding a useState forces the choice; it can no longer be made by accident.",
  "Dashboard.tsx": {
    "chartWidth": "measured from the DOM on mount; the last width is kept in a module-level cache, so a remount starts from it and re-measures",
    "isPeriodPickerOpen": "a sheet is never mid-open across a remount; the period it picks is what persists",
    "isTrendCategoryModalOpen": "a modal is never mid-open across a remount",
    "isTrendSubcategoryModalOpen": "a modal is never mid-open across a remount",
    "tooltipData": "a chart tooltip belongs to a touch that has ended",
    "showGreeting": "governed by a module-level once-per-launch flag, not by the tab",
    "reviewPointerGone": "waving away the pointer to last month's review loses nothing - the review lives on that month permanently, so it need not survive a remount, and the first days of a month may reasonably offer it again",
    "confirmHideInsights": "a confirmation dialog is never mid-open across a remount; the answer it collects is a persisted setting, which is what survives",
    "sharedMode": "the household lens is a place you visit; every launch and every remount opens on the personal view deliberately",
    "trendWidth": "measured from the DOM after mount so the trend chart's viewBox matches the real tile width; a remount re-measures"
  },
  "Activity.tsx": {
    "isCategoryModalOpen": "a modal is never mid-open across a remount",
    "isSubcategoryModalOpen": "a modal is never mid-open across a remount",
    "isSourceModalOpen": "a modal is never mid-open across a remount",
    "isSearchModalOpen": "a modal is never mid-open across a remount",
    "exportScopeOpen": "a sheet is never mid-open across a remount; it asks again next time",
    "renderLimit": "How much of the list is painted, not what the user was looking at: a remount re-paints from the cap by design, and persisting a deep Show more would re-pay the full-paint stall this state exists to avoid.",
    "menuOpen": "the overflow menu is never mid-open across a remount",
    "selectMode": "a remount means the ledger changed under the selection - a sync landed, an occurrence materialised - and rows the user never ticked would now be in it. Selection mode ends rather than carries on over a list that moved.",
    "selected": "ticks belong to the rows that were on screen when they were made; see selectMode",
    "bulkCategoryOpen": "a sheet is never mid-open across a remount",
    "bulkSourceOpen": "a sheet is never mid-open across a remount"
  }
}
